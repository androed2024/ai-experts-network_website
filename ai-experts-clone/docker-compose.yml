services:
  frontend:
    image: nginx:alpine
    container_name: ai-experts-frontend-1
    ports:
      - "127.0.0.1:3001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/usr/share/nginx/html:ro
    restart: unless-stopped
